# Calcite 集成与执行原理

本文记录 MiniDB 与 Calcite 集成过程中的关键原理:子查询去相关(EXISTS / IN / 标量)、null-safe 比较、CAST 与隐式转换。内容基于 2026-08-14 的代码状态。

## 现状(根因)

Calcite 的 `SqlToRelConverter` 默认配置 `expand=false`,把 `EXISTS` / `NOT EXISTS` / `IN (子查询)` / 标量子查询**保留为 `RexSubQuery`**,而不是展开成独立的 RelNode。

`RexSubQuery` 内嵌在 `LogicalFilter` / `LogicalProject` 的条件或投影表达式里,其 `rel` 字段持有一颗**逻辑子树**(`LogicalFilter`/`LogicalTableScan` 等)。相关子查询在这颗子树里通过 `LogicalCorrelVariable`(打印为 `$cor0.id`)引用外层列。

例如 `SELECT * FROM a WHERE EXISTS (SELECT 1 FROM b WHERE b.aid = a.id)` 经 `SqlToRelConverter` 直接产出:

```
LogicalProject(id=[$0], name=[$1])
  LogicalFilter(condition=[EXISTS({
LogicalFilter(condition=[=($1, $cor0.id)])
  LogicalTableScan(table=[[minidb, b]])
})], variablesSet=[[$cor0]])
    LogicalTableScan(table=[[minidb, a]])
```

关键点:**物理转换能通过,但 `RexInterpreter` 没有 `RexSubQuery` 的求值分支**。子查询以 `RexSubQuery` 身份一路流到 `MiniDbFilter`/`MiniDbProject`,执行时走 `RexInterpreter.evalCall` 的 default 分支 → `functions.lookup(...)` 返回 null → 抛 `unsupported operator`。这就是「EXISTS 不支持」的直接根因。

## 解决原理:子查询移除 + 去相关

Calcite 的标准做法(等价于 `Programs.standard()` 的前两步)是**在逻辑优化阶段、物理转换之前**,把 `RexSubQuery` 先提升再拆掉:

1. **`SubQueryRemoveRule`**(`FILTER`/`PROJECT`/`JOIN` 三个 config)——把 Filter/Project/Join 条件里的 `RexSubQuery` 提升为 `LogicalCorrelate` 节点(非相关子查询也可能直接改写为 join)。
2. **`RelDecorrelator.decorrelateQuery`**——把 `LogicalCorrelate` 去相关为普通 join/聚合/过滤,消除 `$cor0` 相关变量。

MiniDB 在 `LogicalOptimizer.optimize` 里、现有 HEP 逻辑规则之前,插入了这两步(`decorrelateSubqueries`)。去相关后的计划**只使用 MiniDB 已有的算子**,不新增任何物理算子,也不需要 SEMI/ANTI join。

### 去相关产物的实际形态

以 `a(id, name)` 与 `b(id, aid)` 两张表为例:

**非相关 `EXISTS (SELECT 1 FROM b WHERE b.id > 5)`** 变成(交叉连接 + 聚合压成单行):

```
LogicalProject(id, name)
  LogicalJoin(condition=[true], joinType=[inner])     -- 交叉连接
    LogicalTableScan(a)
    LogicalAggregate(group=[{0}])                     -- 按常量分组,压成 0 或 1 行
      LogicalProject(i=[true])
        LogicalFilter(b.id > 5)
          LogicalTableScan(b)
```

**非相关 `NOT EXISTS`** 变成(LEFT join + `IS NULL` 过滤):

```
LogicalFilter(condition=[IS NULL($2)])
  LogicalJoin(condition=[true], joinType=[left])
    LogicalTableScan(a)
    LogicalAggregate(group=[{0}]) ...                 -- 子查询非空时产出 1 行,空时 0 行
```

**相关 `EXISTS (SELECT 1 FROM b WHERE b.aid = a.id)`** 变成(等值 join + 按 aid 去重):

```
LogicalJoin(condition=[=($0, $2)], joinType=[inner])
  LogicalTableScan(a)
  LogicalProject(aid=[$0], $f1=[true])
    LogicalAggregate(group=[{0}])                     -- 按 aid 去重(去掉 b.aid 的 NULL)
      LogicalProject(aid=[$1])
        LogicalFilter(IS NOT NULL($1))
          LogicalTableScan(b)
```

**相关 `NOT EXISTS`** 是同一形态,只是 joinType 换成 `left`、外面包 `IS NULL`。这些计划里出现的 `INNER/LEFT join`、聚合、过滤、投影、`=`,MiniDB 全都已经支持。

## 附带改动(去相关暴露出的既有 bug)

去相关让一些之前从不出现的计划形态第一次走到执行器,暴露了三处既有缺陷:

1. **`IS NULL` / `IS NOT NULL` 谓词缺失**。去相关硬依赖它们(`NOT EXISTS` → `IS NULL`、相关 `EXISTS` → `IS NOT NULL`),而 `RexInterpreter` 之前没有这两个 `SqlKind` 分支。已在 `RexInterpreter` 新增 `nullTest` 专用 handler:非 STRICT(操作数为 null 时仍产出 true/false 而非 null),类型无关(任意向量)→ 产出 `BitVector`,逐行读 `isNull` 位。顺带让普通的 `WHERE col IS NULL` 也可用了。

2. **MiniDbJoin 空输入崩溃**。三个 join 子类(`MiniDbHashJoin`/`MiniDbSortMergeJoin`/`MiniDbNestedLoopJoin`)构造 null 填充行时用 `right.get(0).length`,当某侧输入为空(如 `EXISTS` 子查询空)时 `get(0)` 抛 `IndexOutOfBoundsException`。已改为用 `getLeft()/getRight().getRowType().getFieldCount()` 从行类型取列数(不依赖数据)。

3. **MiniDbJoinRule 把交叉连接误判为等值连接**。Calcite 的 `JoinInfo.isEqui()` 只看 `nonEquiConditions` 是否为空,`condition=true`(交叉连接)时两侧 key 都为空却仍返回 true,导致 `EXISTS` 的非相关交叉连接被路由到 `MiniDbSortMergeJoin`。已加 `joinInfo.leftKeys.isEmpty()` 守卫(现落在 `MiniDbHashJoinRule`/`MiniDbSortMergeJoinRule` 的 `convert` 里返回 null),空 key 的连接走 `MiniDbNestedLoopJoin`(任意条件路径)。

## 标量子查询与 IS NOT DISTINCT FROM

标量子查询(`SELECT (SELECT COUNT(*) ... )`)去相关后,相关键的比较用 **`IS NOT DISTINCT FROM` 的 null-safe 等值 join**(而非 `=`),这是为了正确处理「外层键无匹配 → 补 NULL → 再转 0」的语义。为此 `RexInterpreter` 需要两种算子:

1. **`IS_NOT_DISTINCT_FROM` / `IS_DISTINCT_FROM`**——由 `RelDecorrelator` 在 join 条件里直接产出,类型无关、非 STRICT(两侧都 null → 取决于操作符;一侧 null → 取反;都非 null → `Objects.equals` 判等)。
2. **`IS TRUE` / `IS NOT TRUE` / `IS FALSE` / `IS NOT FALSE`**——`SqlToRelConverter` 把直接写在 WHERE 里的 `x IS NOT DISTINCT FROM y` **解析期改写**为 `IS TRUE(x = y)`(`IS DISTINCT FROM` → `IS NOT TRUE(x = y)`),所以直接谓词走这一族,而非上面的 `IS_NOT_DISTINCT_FROM`。二者缺一不可。

## Calcite 的 CAST 与隐式转换

Calcite 对「类型不一致的表达式」有两类处理,落点不同,极易搞混:

### 1. 显式 CAST(`CAST(x AS type)`)

`CAST` 是一个 `RexCall`(`SqlKind.CAST`),`RexInterpreter.evalCall` 有专用 `case CAST` 分支(`evalCast`)。它按目标 `SqlTypeName` 分配对应 Arrow 向量,把源值读出来转换:

- 数值目标(INTEGER/BIGINT/DOUBLE/DECIMAL…)用 `asLong`/`asDouble` 读源值(会 parse 字符串,如 `CAST('123' AS INTEGER)`)。
- 字符串目标用 `asString` 把数值转字符串。
- 布尔目标用 `asBoolean`。

### 2. 隐式转换(混型表达式)

当两个操作数类型不一致(如 `int_col + double_col`、`int_col > double_col`),Calcite 做类型推导决定结果类型,但**是否插入显式 CAST 因算子而异**:

| 算子 | 是否插 CAST | 例子 |
|------|-------------|------|
| 比较 `=` `>` `<` 等 | **插**,两侧 CAST 到同型 | `i > d` → `>(CAST($0):DOUBLE, $1)` |
| 算术 `+ - * /` | **不插**,保留混型 | `i + d` → `+($0, $1)` |

这个不对称是本次的坑:比较只需处理「同 family 的 INT↔BIGINT 混型」(同属 integer family,Calcite 不 CAST);算术则必须为「跨 family」(INT↔DOUBLE/DECIMAL 等)也补重载。

### 3. 执行侧如何承接

`RexInterpreter` 求值一个 `RexCall` 时,先 `eval` 各操作数成向量,再按 operator 走两条路:

- **专用 handler**(CAST / CASE / TRIM / IS NULL / IS NOT DISTINCT FROM 等)在 `evalCall` 的 `case` 分支内联处理。
- **其余**走 `exec/functions` 注册表(`functions.lookup`),由 `Function` 按「输入向量类型 + 输出类型」分发到某个 `Overload.kernel`。

因此隐式转换的落地方式就是**在 `BuiltInFunctions.arithmeticFunction` 注册足够的跨型重载**:整型×DOUBLE→DOUBLE、整型×DECIMAL→DECIMAL、DOUBLE×DECIMAL→DOUBLE。用两个通用核 `fillDoubleMixed`/`fillDecimalMixed` 把两侧分别提升到 `double`/`BigDecimal`,再复用 `doubleKernel`/`decimalKernel` 按更宽类型计算,避免为每种组合手写一遍 `+ - * /`。

## 范围边界

- 本次支持:**EXISTS / NOT EXISTS**(相关 + 非相关)、**标量子查询**(相关)、**IN (子查询)**(非相关)。
- `IS NOT DISTINCT FROM` / `IS DISTINCT FROM` 作为谓词也直接可用(经 `IS TRUE`/`IS NOT TRUE` 改写)。

