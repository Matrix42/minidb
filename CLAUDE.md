# CLAUDE.md — MiniDB 项目指南

本文件供 Claude Code 在后续会话中读取,以快速理解项目、节省 token。内容基于 2026-08-11 的代码状态;若代码与本文冲突,以代码为准。

## 项目概览

MiniDB 是一个基于 Apache Calcite(解析/规划)+ Apache Arrow(列式存储)+ Netty(网络)的自研微型 JDBC 数据库。三模块:

- `minidb-protocol` — Netty wire 协议(Message 编解码),**极简且稳定,改动需极谨慎**。
- `minidb-server` — 服务端:Calcite 解析/规划、Arrow 存储、向量化批式执行、JDBC 协议处理。
- `minidb-jdbc` — 客户端 JDBC 驱动(`jdbc:minidb://host:port`),基于自定义 Netty 协议。

**定位**:功能完整的单机数据库。无事务(autoCommit 恒 true)。

## 构建与运行

- **JDK 17 必须**。`JAVA_HOME` 指向 JDK 17(本机为 `C:\Users\Matrix42\.jdks\azul-17.0.15` 或 `C:\Program Files\Java\jdk-17`)。
- **构建命令**(bash 下直接跑 `./mvnw.cmd`,不是 `mvnw.cmd` 也不是 `cmd //c`):
  - 全量测试:`./mvnw.cmd test`
  - 单模块测试:`./mvnw.cmd test -pl minidb-server`
  - 单测试类:`./mvnw.cmd test -pl minidb-server -Dtest=QueryExecutorTest`
  - 编译:`./mvnw.cmd -pl minidb-server -am compile -q`
- **启动服务端**:`./mvnw.cmd -pl minidb-server exec:java`,默认监听 8899,数据目录 `./data`。需加 JVM 参数:
  ```
  --add-opens=java.base/java.nio=ALL-UNNAMED
  --add-opens=java.base/jdk.internal.misc=ALL-UNNAMED
  --add-opens=java.base/sun.nio.ch=ALL-UNNAMED
  ```
- **JDBC 客户端**需加:`--add-opens=java.base/java.nio=org.apache.arrow.memory.core,ALL-UNNAMED`(Arrow 的 MemoryUtil 用了 Unsafe)。

## 项目规范(重要)

1. **改完代码就提交**。用 conventional commit 风格(`feat:`/`fix:`/`test:`/`refactor:`/`docs:`),不要 amend,不要 `--no-verify`。
2. **在 `master` 分支工作**。功能分支命名 `<feature-name>`(如 `explain-analyze`),完成后 fast-forward 合并回 `master` 并删除功能分支。本仓库无远程(纯本地),无 PR 流程。
3. **小步提交**:一个逻辑改动一个 commit,便于回溯。
4. **代码是给人读的,不只是给 AI 读**。命名必须自解释:不用无意义的短缩写(`lk`/`rn`/`i2`/`j2` 这类),用描述性名字(`leftKeyCols`/`leftHasNullKey`/`leftScanOrder` 等)。注释解释 WHY 而非复述 WHAT(WHAT 由好命名承担);非显然的逻辑(如 null 键在等值 join 永不匹配、输入已有序时跳过内部排序)必须加注释说明原因。重构时顺带清理死代码与冗余计算。
5. **测试用 JUnit 5 + `@TempDir` + `RootAllocator`**。断言关系/比例而非精确浮点值(选择率单元测试除外,用 1e-9 delta)。
6. **现有 7 个物理算子文件(`MiniDbScan`/`Filter`/`Project`/`Sort`/`Values`/`Modify`/`Aggregate`)和 `minidb-protocol` 模块尽量不改**——它们是稳定核心,扩展应通过新模块(参考 EXPLAIN 用 `ExplainExecutor` + `Instrumenter` 外挂的方式,零侵入算子)。规则类在 `rule/physical` 包(逻辑优化规则在 `rule/logical`)。
7. **用中文回复用户**(代码/标识符/路径保持原文)。
8. **不要偷懒,优先做对而不是做快**。当「干净、可扩展」的改法可行时,不要用局部特判/硬编码糊过去——特判会在调用点留 wart、破坏统一性、堵死后续同类扩展。判据:如果「把这个特判复制第二遍」会让你想把它下沉成框架能力,那第一次就不该特判。反例(已修):`CURRENT_DATE`/`CURRENT_TIMESTAMP` 最初在 `RexInterpreter.evalCall` 按算子特判,正确做法是让 `Function.evaluate` 支持 0 参函数,再把它们当普通函数注册进 `BuiltInFunctions`。

## 架构与关键类

### SQL 执行流水线
```
QueryExecutor.execute(sql, currentSchema)
  → 顶部前缀识别(EXPLAIN / EXPLAIN ANALYZE / ANALYZE / USE SCHEMA,在 calcite.parse 之前)
  → schema DDL(SqlCreateSchema / SqlDropSchema,calcite.parse 之后)
  → CalciteContext.parse(sql) → SqlNode
  → Planner.plan(sql, currentSchema) → RelNode(两阶段:LogicalOptimizer 逻辑优化 → VolcanoPlanner + MiniDbPhysicalRules 物理转换)
  → MiniDbRel.execute(ExecContext[currentSchema]) → BatchIterator(拉模式,批式向量化)
  → QueryResult.Rows / Update / UseSchema
  → SessionHandler[per-channel currentSchema] → Message.ArrowBatch / UpdateCount → wire → 客户端
```

### 关键类(`minidb-server/src/main/java/com/minidb/server/`)

**执行核心:**
- `exec/QueryExecutor` — SQL 入口,前缀分发(EXPLAIN / EXPLAIN ANALYZE / ANALYZE / USE SCHEMA,在 calcite.parse 之前)+ schema DDL(`SqlCreateSchema`/`SqlDropSchema`)+ 限定名 CREATE/DROP/TRUNCATE + 规划执行。4 参构造:`(catalog, storage, allocator, stats)`。`execute(sql)` 委托 `execute(sql, "public")`;`execute(sql, currentSchema)` 主路径,currentSchema 作参数流经 Planner/CalciteContext(不落共享成员字段,避免跨连接污染)。USE SCHEMA 返回 `QueryResult.UseSchema(name)`。
- `exec/ExecContext` — 执行上下文(`storage` + `allocator` + `RexInterpreter` + `currentSchema`),每查询新建。`getTable(schema,table)`/`markDirty(schema,table)` schema 感知;裸名 `getTable(table)`/`markDirty(table)` 用 `currentSchema` 解析(供算子的 size==2 路径)。**瞬态表注册表**(`Map<String,List<Object[]>>`,per-query 单线程):`putTransientTable`/`transientTable`/`removeTransientTable`,供递归 CTE 的 `MiniDbRepeatUnion` 写、`MiniDbScan` 读 working 行。
- `exec/BatchIterator` — 拉模式批迭代器接口(`hasNext`/`next`/`close`,`empty()` 工厂)。
- `exec/QueryResult` — sealed 接口:`Rows(VectorSchemaRoot)` / `Update(long)` / `UseSchema(String schemaName)`(USE SCHEMA 切换,SessionHandler 据此更新 per-channel currentSchema)。
- `exec/RexInterpreter` — RexNode 表达式求值**薄壳**:只处理 RexInputRef(拷列)/RexLiteral(字面量向量)求值 + AND/OR/NOT/CAST/CASE/TRIM 专用 handler;算术/比较/字符串/数学函数一律经 `functions.lookup(call.getOperator())` 走 `exec/functions` 框架(见坑 47),供 Filter/Project 用。双参构造 `(allocator, FunctionRegistry)` 注入自定义注册表(UDF 扩展点),单参构造默认 `BuiltInFunctions.newRegistry()`。TRIM 是专用 handler 因为其 3 参形式带 SYMBOL 标志字面量(非列值)。
- `exec/RowCopier` — 跨 VectorSchemaRoot 按行/按值拷贝。

**规划与算子(`plan/` + `rule/` 四包子结构):**
- `plan/Planner` — 编排,**两阶段规划**:先 `LogicalOptimizer.optimize(logical)`(HepPlanner 逻辑优化)→ 再 VolcanoPlanner 物理转换。`plan(sql)` 委托 `plan(sql, "public")`,透传 currentSchema 给 `CalciteContext.planInCluster`。物理阶段注册 `ConventionTraitDef` + `RelCollationTraitDef`(先于 `RelOptCluster.create`)+ `MiniDbPhysicalRules.ALL`(12 个 ConverterRule),`changeTraits(MiniDbConvention.INSTANCE)` → `findBestExp()` 产出 MiniDbRel。
- `plan/logical/LogicalOptimizer` — 逻辑阶段:HepPlanner 直接优化 Calcite Logical* 树(不自建逻辑节点),跑 `rule/logical` 规则集,返回 `findBestExp()`。
- `rule/logical/MiniDbLogicalRules` — 逻辑规则集(Calcite 内置,`Config.DEFAULT.toRule()` 构造),分两组:
  - `HEP`(HepPlanner 阶段,不依赖 trait,Filter/Project 标准路径):`FilterJoinRule.FilterIntoJoinRule`(FilterPushDown 进 join)/`FilterProjectTransposeRule`/`ProjectMergeRule`/`FilterMergeRule`/`FilterSetOpTransposeRule`/`ProjectSetOpTransposeRule` + `FilterAggregateTransposeRule`/`ProjectAggregateMergeRule`/`AggregateProjectMergeRule`/`AggregateRemoveRule`(聚合下推/合并/消除)+ `FilterSortTransposeRule`/`SortProjectTransposeRule`(排序换位)+ `UnionEliminatorRule`(单输入 UNION 消除)+ `SortRemoveConstantKeysRule`/`SortRemoveDuplicateKeysRule`/`SortRemoveRedundantRule`(排序化简)。
  - `SORT`(VolcanoPlanner 阶段,依赖 `RelCollationTraitDef`):`SortRemoveRule`(输入已满足 collation 时移除冗余 Sort)。
- `plan/physical/`(`com.minidb.server.plan.physical`)——MiniDb* 物理算子,均 `implements MiniDbRel`:
  - `MiniDbRel` — 接口:`BatchIterator execute(ExecContext)`。每个算子自己 `execute()` 内部调子算子 `execute()`(拉模式)。
  - `MiniDbScan` — 表扫描,返回表 owned 的 batches(close 是 no-op,batches 归表所有)。**瞬态表路径**:限定名只有 1 段(递归 CTE 的 `TableScan(nums)`)时,查 `ctx.transientTable(name)`,命中则用 `RowVectors.buildRoot` 物化 working 行为单批(owned,close 释放)。
  - `MiniDbFilter` — 过滤,自己分配输出 batch,`owned` 队列跟踪待关闭。
  - `MiniDbProject` — 投影,RexInterpreter 求值后重命名列。窗口函数(RexOver)走 eager 路径(见 WindowFunctions)。
  - `MiniDbSort` — **eager**:`execute()` 内全量物化+排序,返回惰性迭代器。offset/fetch 字面量处理。
  - `MiniDbValues` — VALUES 字面量。
  - `MiniDbModify` — INSERT/UPDATE/DELETE,写路径调 `storage.markDirty(tableName)`(此处触发 stats stale 钩子)。
  - `MiniDbAggregate` — 聚合,`Aggregate` 子类。**eager**:`execute()` 拉取输入全量,流式分组聚合,输出单批。分组 key 为 `List<Object>` 规范化值(含 null),`LinkedHashMap` 保首见顺序;每 `AggregateCall` 一个 `Accumulator`(COUNT=long,SUM 按参数 long/double,AVG=sum+count,MIN/MAX=Comparable;`DISTINCT` 聚合用 `DistinctAcc` 维护 `LinkedHashSet` 去重后按类型聚合)。NULL 语义:聚合忽略 NULL;`COUNT(*)` 计所有行;空输入无 GROUP BY → 1 行(COUNT=0 其余 NULL);有 GROUP BY 空表 → 0 行。输出列类型按 `getRowType()`(Calcite 推导)。`SELECT DISTINCT` 由 Calcite 规划为无 aggCalls 的分组聚合,天然支持。
  - `MiniDbUnion` — UNION/UNION ALL,`Union` 子类。**eager**:先收集所有输入 batches 再 merge(必须 merge 后才 `close()` 输入迭代器,否则 Project/Filter owned batch 被释放)。`all=true` 级联;`all=false` 用 `LinkedHashSet<List<Object>>` 行级去重。输出单批,`VectorSchemaRoot.of()` 前必须 `setValueCount`(of 的 rowCount 取第一个 vector 的 valueCount)。
  - `MiniDbSetOp` — INTERSECT/EXCEPT,`SetOp` 子类,kind 字段区分(`SqlKind.INTERSECT`/`MINUS`,SetOp 自带 kind+all,无 INTERSECT_ALL 枚举)。**eager**:每输入统计行级 key 计数(LinkedHashMap 保序),INTERSECT 取跨输入 min 计数、EXCEPT 取 count0-Σrest,>0 保留;`all=false` 每 key 输出 1 行、`all=true` 输出 n 行。输出顺序 = 第一输入首见顺序。
  - `MiniDbCalc` — Calc(Project+Filter 泛化),`Calc` 子类,**防御性算子**:当前 Filter/Project 标准路径下常规 SQL 不产生 LogicalCalc(全部 Project/Filter),未来若有 Calc 节点即可处理。**lazy 流式**:`program.getProjectList()` 逐个 `expandLocalRef` + `getCondition()`,eval 求值,按条件行过滤后 `renameFiltered` 拷入新向量。**eager 窗口路径**(防御):投影含 `RexOver` 时物化、先按 condition 过滤、算窗口列、eval 改写投影。
  - `MiniDbJoin` — JOIN,`Join` 子类,**抽象基类**:`execute()` 编排(eager 物化两侧为 `List<Object[]>` → 抽象 `joinRows(left,right,info,type,ctx)` → `buildOutput` 单批惰性迭代器),共享辅助方法(`materialize`/`readObject`/`writeObject`/`keyOf`/`concat`/`compareKeys`/`sortedIndices`/`containsNull`);`coversKeys(List<RelCollation>, List<Integer>)` 判 collation 是否升序前缀覆盖 join 键。INNER/LEFT/RIGHT/FULL 支持,SEMI/ANTI 抛错。输出单批,getRowType()=左+右。三个实现类:
    - `MiniDbHashJoin` — 等值:HASH 左建表右探测(NULL 键等值不匹配)。
    - `MiniDbSortMergeJoin` — 等值 + 双侧输入有序:构造时 `RelMetadataQuery.collations(input)` 经 `coversKeys` 检查 collation 覆盖 join 键,已覆盖侧用输入序(identity,跳过内部排序)、未覆盖侧才 `sortedIndices`(null 排最后);`leftInputSorted()`/`rightInputSorted()` 暴露判断结果。
    - `MiniDbNestedLoopJoin` — 任意条件:逐对行构造 1 行 probe root eval 全条件(正确性优先)。
  - `MiniDbRepeatUnion` — 递归 CTE(WITH RECURSIVE),`RepeatUnion` 子类(seed+iterative 两输入)。**eager 迭代不动点**:materialize seed → 逐轮把 working 行注册进 `ExecContext` 瞬态表(名=transientTable 限定的最后一段)→ materialize iterative 项(其内部 `MiniDbScan(单段名)` 从瞬态表读回 working)→ 去重后 append、working=新行,直到不产新行或达 `iterationLimit`。`all=true`(UNION ALL)不建 seen 集;`all=false`(UNION)用 `LinkedHashSet<List<Object>>` 全局去重。输出单批。
  - `MiniDbTableSpool` — 递归 CTE 的 spool,`TableSpool` 子类,**纯透传**:`execute()` = `getInput().execute(ctx)`。真正的 spooling(累加每轮行到瞬态表)由 `MiniDbRepeatUnion` 驱动,spool 节点只负责让 Calcite 的 `LogicalTableSpool` 能转换到 MINIDB 约定。
  - `RowVectors` — 行↔Arrow 转换静态工具(`materialize`/`readObject`/`writeObject`/`buildRoot`),供 `MiniDbRepeatUnion` 与 `MiniDbScan` 瞬态读取复用。同一套 `readObject`/`writeObject` 也私有存在于 MiniDbJoin/MiniDbUnion/WindowFunctions(未合并,历史遗留)。
  - `WindowFunctions` — 窗口函数静态工具:`materialize` + `computeOver(RexOver, rows)`。按 RexWindow 的 partitionKeys 分组、orderKeys 排序,逐行计算:聚合 SUM/AVG/COUNT/MIN/MAX over 帧、ROW_NUMBER/RANK/DENSE_RANK(peers)、LEAD/LAG(offset+default)、FIRST_VALUE/LAST_VALUE。帧=RexWindowBound 转行位置(RANGE/ROWS 同位置语义)。**窗口执行在 MiniDbProject 内嵌(MiniDbCalc 兜底)**:检测投影含 RexOver → eager 路径,`RexShuttle.visitOver` 提取为窗口列引用后 eval 改写表达式。Calcite 1.42 无 LogicalWindow(ProjectToWindowRule 不可靠),无窗口算子/规则。
- `rule/physical/`(`com.minidb.server.rule.physical`)——每个规则一个类:`MiniDbScanRule`/`MiniDbFilterRule`/`MiniDbProjectRule`/`MiniDbSortRule`/`MiniDbValuesRule`/`MiniDbModifyRule`/`MiniDbAggregateRule`/`MiniDbUnionRule`/`MiniDbIntersectRule`/`MiniDbExceptRule`/`MiniDbCalcRule`/`MiniDbHashJoinRule`/`MiniDbSortMergeJoinRule`/`MiniDbNestedLoopJoinRule`/`MiniDbTableSpoolRule`/`MiniDbRepeatUnionRule`,均 `extends ConverterRule`,构造器链:`this(Config.INSTANCE.withConversion(...).withRuleFactory(XxxRule::new))` + 私有 `(Config)` 构造器 `super(config)`。join 由三条规则按代价选:非等值(或交叉连接)只有 `MiniDbNestedLoopJoinRule` 适用(Hash/SortMerge 规则在 `convert` 里对 `!isEqui() || leftKeys.isEmpty()` 返回 null);等值则三条都产出备选,靠各 `MiniDbJoin` 子类的 `computeSelfCost`(见坑 52)排名,VolcanoPlanner 选最便宜。`MiniDbPhysicalRules`(原 `MiniDbRules` 更名,与 logical 侧对称)聚合引用。

**存储(`storage/`):**
- `storage/StorageManager` — 表目录 + Arrow IPC 持久化。**schema 感知**:存储 key 为 `schema.table`(小写),文件路径 `data/<schema>/<table>.arrow`(子目录),`flushTable` 必须 `createDirectories(file.getParent())`。`loadAll` 两级目录遍历,从 Arrow schema metadata 恢复 schemaName。`getTable(schema,table)`/`dropTable(schema,table)`/`markDirty(schema,table)`/`truncateTable(schema,table)`/`dropSchema(name)`(级联删表+文件,catalog 抛 public/missing);裸名重载委托 `public`。持有 `volatile StatsManager` 引用,`markDirty` 触发 `markStale`(key 为 `schema.table`),`dropTable`/`dropSchema` 触发 `dropStats`。
- `storage/ArrowTable` — 单表:`CopyOnWriteArrayList<VectorSchemaRoot>` batches。构造 Arrow `Schema` 时附 `{"schema" → schemaName}` metadata(跨 IPC 流到客户端 `getSchemaName()`)。`rowCount()`(O(批数)非 O(行数))、`batches()`、`newBatchRoot()`、`appendBatch`(MAX_BATCH_ROWS=4096)、`replaceBatches`、`clear`。
- `storage/CatalogStore`(接口)+ `storage/JsonCatalogStore`(实现)——元数据独立持久化接口/JSON 实现,写 `data/catalog.json`(见坑 49)。`StorageManager` 构造时挂 `catalog.addListener(this::persistCatalog)`,DDL 变更即落盘。

**目录(`catalog/`):**
- `catalog/MiniDbCatalog` — 线程安全 schema/table 元数据:`ConcurrentHashMap<schema-lowercase, ConcurrentHashMap<table-lowercase, TableSchema>>`。构造自动建 `"public"` **并注册 `information_schema` 系统 schema**(见 `InformationSchemaCatalog`)。`createSchema`/`dropSchema`(public 与 information_schema 均不可删,级联删表)/`schemaNames`/`tableNames(schema)`/`getTable(schema,table)`/`hasTable(schema,table)`/`dropTable(schema,table)`;裸名重载(`getTable(name)`/`hasTable(name)`/`tableNames()`/`dropTable(name)`)委托 `public`,向后兼容。`snapshot()`/`restore(CatalogSnapshot)` 供持久化(restore 不通知,见坑 49;snapshot 排除系统 schema)。
- `catalog/CatalogSnapshot` — `record(List<String> schemas, List<TableSchema> tables)`,格式无关的 catalog 快照(避免 catalog→storage 循环依赖)。
- `catalog/InformationSchemaCatalog` — `information_schema` 系统 schema 元数据:`SCHEMA_NAME="information_schema"`、`isSystemSchema(name)`、`schemataSchema()/tablesSchema()/columnsSchema()`(固定 `TableSchema`,列名标准大写)、`tables()`(3 表列表)。供 `MiniDbCatalog` 注册、`MiniDbScan`/`QueryExecutor` 判系统 schema、`exec/InformationSchema` 物化。
- `catalog/TableSchema` — `record(schemaName, name, List<ColumnMeta>)`;便捷构造 `TableSchema(name, cols)` 委托 `schemaName="public"`。`columnIndex` 大小写不敏感。
- `catalog/ColumnMeta` — `record(name, ColumnType)`。
- `catalog/ColumnType` — 枚举:INTEGER/BIGINT/DOUBLE/VARCHAR/BOOLEAN/DATE/TIMESTAMP。
- `catalog/ArrowTypes` — SqlTypeName↔ColumnType↔ArrowType 互转,`field(...)` 工厂。

**Calcite 集成(`calcite/`):**
- `calcite/CalciteContext` — parser 配置(MYSQL lex,大小写不敏感,DDL parser factory)。`parse(sql)` → SqlNode;`planInCluster(sql, cluster, currentSchema)` → RelRoot(旧 2 参重载委托 `public`)。catalog reader 搜索路径 `["minidb"]`;`MiniDbRootCalciteSchema` 挂在 `minidb` 下,其 `getTableMap()` 返回当前 schema 的表(支持 unqualified 名),`getSubSchemaMap()` 返回所有 schema(支持 `schema.table` 限定名,含 `information_schema`,统一从 `catalog.schemaNames()` 派生)。**列名大小写不敏感靠 `CalciteCatalogReader` 的连接属性 `caseSensitive=false`**(见坑 48)。
- `calcite/MiniDbRootCalciteSchema` — 容器 schema,接 `(catalog, currentSchema)`,既暴露当前 schema 的表也暴露所有子 schema(含 `information_schema`)。
- `calcite/MiniDbCalciteSchema` — 单 schema 实例,`getTableMap()` 返回该 schema 的表。
- `calcite/MiniDbCalciteTable` — 把 catalog 表暴露给 Calcite。

**统计(`stats/`,EXPLAIN 附属子系统):**
- `stats/Histogram` — 不可变单列等频直方图(10 桶 + top-10 MCV + distinct/null/total)。`selectivity(RexNode, long)` 选择率模型(等值/范围/AND/OR/NOT/默认0.33)。**Serializable**。跨列类型不兼容时 rangeSelectivity 走 `DEFAULT_SELECTIVITY` 兜底(不抛 ClassCastException)。
- `stats/HistogramBuilder` — 从 `List<ValueVector>` 建 Histogram(排序+等频分桶+MCV)。
- `stats/StatsManager` — 管理所有表的 `TableStats`,`analyze`/`analyzeAll`/`tableStats`/`markStale`/`dropStats`/`loadAll`。`.stats` 文件 Java 序列化持久化,`read()` 失败 LOG.warn。`AutoCloseable`(close no-op)。
- `stats/TableStats` — `record(Map<String,Histogram> columnHistograms, boolean stale)`,Serializable。map 键为小写列名。

**EXPLAIN(`exec/`):**
- `exec/ExplainExecutor` — `explain(sql, currentSchema)`(估算,不执行)/`analyze(sql, currentSchema)`(插桩执行),透传 currentSchema 给 `planner.plan`(旧单参重载委托 public)。7 列结果集:`id INT, parent_id INT nullable, operation VARCHAR, rows BIGINT nullable, batches INT nullable, elapsed_ms DOUBLE nullable, remarks VARCHAR`。plain 和 ANALYZE 都折叠 Calcite 插入的 trivial Project 节点,保证两者行集一致。内部 `storage.getTable(table)` 用裸名,非 public 表的统计降级(坑 18)。
- `exec/Instrumenter` — ANALYZE 用:用各算子 `copy()` 构造插桩影子树,`InstrumentedRel extends AbstractRelNode implements MiniDbRel` 包裹每个节点,`execute()` 返回的 measured 迭代器在 `next()` 累加 rows/batches、`close()` 记 elapsed。`start` 必须在 `wrapped.execute()` 之前设置(否则 Sort 的 eager 物化时间不计)。stats 按**原始**节点 keyed 到 `IdentityHashMap`。
- `exec/NodeStats` — 可变 `long rows; int batches; double elapsedMs;`。
- `exec/InformationSchema` — 只读 `information_schema` 系统表的行物化:`materialize(catalog, tableName, allocator) → VectorSchemaRoot`(从内存 catalog 物化 schemata/tables/columns 行)。表定义(schema 名 + 列)在 `catalog/InformationSchemaCatalog`,这里只负责把行填进 Arrow 向量(见坑 50)。

**网络:**
- `MiniDbServer` — 启动:`storage.loadAll()` → `StatsManager` 构造 + `setStatsManager` + `loadAll` → `QueryExecutor` → Netty `ServerBootstrap`。
- `netty/SessionHandler` — 持有 per-channel `currentSchema` 字段(默认 public)。`handleExecute`:调 `executor.execute(sql, currentSchema)` → `QueryResult.UseSchema` 更新自身 currentSchema 并回 `Message.UpdateCount(0)`;`Rows` 走 `sendRows`(Arrow IPC 编码成 `Message.ArrowBatch`)然后 `close()`;`Update` 走 `Message.UpdateCount`。异常 → `Message.ExecuteResponse.error`。**注意**:`rows.data().close()` 不在 finally 里(pre-existing,所有 Rows 路径都这样,非 EXPLAIN 特有)。
- `MetadataExecutor`(外挂,`catalog+allocator`)服务 `getSchemas`/`getTables`/`getColumns` 协议请求,`SessionHandler.handleMetadata` 走 `sendRows`。

**协议(`minidb-protocol`):**
- `Message` — sealed records:`Handshake`/`HandshakeAck`/`ExecuteRequest(requestId, sql)`/`SchemasRequest(requestId, schemaPattern)`/`TablesRequest(requestId, schemaPattern, tableNamePattern, types)`/`ColumnsRequest(requestId, schemaPattern, tableNamePattern, columnNamePattern)`/`CloseRequest`/`ExecuteResponse`/`ArrowBatch(requestId, lastBatch, data)`/`UpdateCount(requestId, count)`。
- `MessageType` — byte 常量。`MessageEncoder`/`MessageDecoder` — Netty 编解码。

**JDBC 客户端(`minidb-jdbc/`):**
- `MiniDbClient` — Netty 客户端,`execute(sql) → ClientResult.Rows/Update`。按 requestId 路由响应(`ConcurrentHashMap<Long, CompletableFuture>`),连接断开时 failAllPending。
- `MiniDbConnection`/`MiniDbStatement`/`MiniDbResultSet`/`MiniDbPreparedStatement`(客户端参数替换)。
- 客户端**不引用** `QueryExecutor`(纯通过网络),所以服务端构造器变化不影响 minidb-jdbc。

## 踩过的坑(经验教训)

1. **`./mvnw.cmd` 在 bash 下直接跑**,不要用 `mvnw.cmd`(PATH 找不到)、`cmd //c mvnw.cmd`(输出编码乱)、`mvn`(没装)。就是 `./mvnw.cmd ...`。
2. **Calcite 默认语法无 `ANALYZE` 关键字**。`EXPLAIN`/`EXPLAIN ANALYZE`/`ANALYZE` 必须在 `calcite.parse` 之前字符串前缀拦截(`QueryExecutor.execute` 顶部)。前缀匹配顺序:`EXPLAIN ANALYZE ` 必须先于 `EXPLAIN `,`ANALYZE`(exact)先于 `ANALYZE <table>`。
3. **Calcite 总是插入 `MiniDbProject`** 做列选择(`SELECT id,name FROM ...`)。EXPLAIN 要折叠 trivial Project 否则行数与预期不符;但 `estimate` 仍要遍历真实树(含 Project)算 childRows。
4. **`MiniDbSort.execute()` 是 eager**——物化+排序在 `execute()` 内完成,返回惰性迭代器。插桩计时 `start` 必须在 `wrapped.execute()` 之前,否则 Sort 的耗时漏掉。Sort 在物化后会 `input.close()`(此时子节点 elapsed 被记录)。
5. **Scan 的 batch 所有权与迭代器 close**——`MiniDbScan.execute()` 直接返回 `tableHandle.scan()` 迭代器,**batch 归迭代器所有,不归表所有**:SimpleTable.scan/MergeScanIterator(LSM) 的 `next()` 把读出的 batch 累积到内部 list,`close()` 统一释放。故 EXPLAIN ANALYZE 等不能 `b.close()` 发出的 batch(会被迭代器 close double-close 或破坏释放),靠 `it.close()` 级联释放。**LSM MergeScanIterator 的坑(已修)**:`next()` 原在返回新批前 close 上一批,但上一批已返回调用方(materializeColumns 收进 batches list 后 copyRow),use-after-close 致 copyRow 读已释放 buffer + 释放链断裂残留内存(每条 join 残留 16MB,查询间累积撑爆 allocator);修为累积到 emitted list、close 统一释放。`MiniDbSort`/`MiniDbFilter` 分配自己的输出 batch 并在 `close()` 关闭。
6. **`FieldVector` 只有 no-arg `allocateNew()`**;要预设容量用 `setInitialCapacity(n); allocateNew();`,不是 `allocateNew(n)`。
7. **`Histogram` 的选择率是单列模型**——`ExplainExecutor.histogramForCondition` 按条件里第一个 `RexInputRef` 选列的直方图,然后对**整个**条件求选择率。复合跨列条件(如 `id>1 AND name<'m'`)对非匹配列走 `DEFAULT_SELECTIVITY` 兜底(已加 `typesCompatible` 守卫,不会抛 ClassCastException)。等值用 `Objects.equals`(类型安全)。
8. **`Histogram.compareValue` 泛型擦除坑**:`(Comparable<Object>)(Comparable<Double>)x` javac 拒绝(Comparable 不变),要 `(Comparable<Object>)(Comparable)x` raw 中转,加 `@SuppressWarnings`。
9. **`Histogram.rangeSelectivity` 插值**:用 `Double.compareTo` 只返回 -1/0/1,不能当数值差用——必须用 `numericDelta` 算真实差值再除以 `spanSize`。否则多单位桶插值全错。
10. **持久化统计用 Java 序列化**——`Histogram`/`Bucket`/`McValue`/`TableStats` 都要 `implements Serializable` + `serialVersionUID`。类字段变更会使旧 `.stats` 失效(`read()` 捕获后 LOG.warn 返回 null,降级为无统计)。
11. **`QueryExecutor` 4 参构造**`(catalog, storage, allocator, stats)`——移除旧 3 参构造会断 `MiniDbServer` 编译,改构造器时要同步改 `MiniDbServer.start()`。
12. **minidb-jdbc 的 `NoClassDefFoundError` 测试失败是环境问题**(Calcite 不在测试 classpath),与服务端改动无关——minidb-jdbc 不引用任何服务端类。别误判为回归。
13. **stale 钩子集中在 `StorageManager.markDirty`**——INSERT/UPDATE/DELETE/TRUNCATE 全走 `markDirty`,所以只需在那里加一次 `markStale`。`MiniDbModify` 的 no-match 早返回路径(`rewriteTable` 行 96)正确跳过 `markDirty`(表未变)。
14. **断开连接的快速失败**:`MiniDbClient`/`MiniDbConnection.isClosed()/isValid()` 依赖 `channelInactive` 标记 `connected=false`,连接池不用试查询就知道连接死了。
15. **`currentSchema` 不能放共享成员字段**——`QueryExecutor` 是所有连接共享的单例,`CalciteContext`/`Planner` 是其 final 成员。若 `currentSchema` 是 `CalciteContext` 可变字段,一个客户端 `USE SCHEMA` 会污染所有并发连接。必须作参数流经 `QueryExecutor.execute(sql, currentSchema)` → `Planner.plan(sql, currentSchema)` → `CalciteContext.planInCluster(sql, cluster, currentSchema)`。`SessionHandler` 持有 per-channel `currentSchema` 字段(默认 public),`USE SCHEMA` 返回 `QueryResult.UseSchema` 让其更新。
16. **`SqlIdentifier.getSimple()` 对复合名静默返回首段**——断言关闭时(JVM 默认)`getSimple()` 对 `public.users` 不抛而返回 `"public"`。限定名解析必须用 `node.name.names`(ImmutableList)分解:schema=`names.get(0)`,table=`names.get(names.size()-1)`。
17. **Calcite schema 树"提升表"副作用**——`MiniDbRootCalciteSchema.getTableMap()` 把当前 schema 的表暴露在 `minidb` 容器层(让 unqualified `t` 解析),导致 `RelOptTable.getQualifiedName()` 对 unqualified 表返回 `["minidb","t"]`(2 段,丢失 schema),对 `other.t` 返回 `["minidb","other","t"]`(3 段)。`MiniDbScan`/`MiniDbModify` 据此分流:`size>=3` 用倒数第二段作 schema,`size==2` 调 `ctx.getTable(裸名)` 由 `ExecContext.currentSchema` 解析。这是算子适配 schema 的唯一改动点(2/6 算子)。
18. **持久化子目录 + StatsManager key 语义**——文件路径 `data/<schema>/<table>.arrow`,`flushTable` 必须 `createDirectories(file.getParent())`(旧扁平格式不兼容)。`StatsManager` 零代码改动但 key 语义从 `table` 变 `schema.table`,`resolveKey(table)` 兼容裸名(默认 public)。`StatsManager.analyze` 当前只支持 public 表(`storage.getTable(DEFAULT_SCHEMA, table)`),非 public 表的 EXPLAIN ANALYZE 统计降级为无统计——分阶段交付,可接受。
19. **JDBC 元数据走专用协议消息**——`getSchemas`/`getTables`/`getColumns` 不复用 `ExecuteRequest`+伪SQL,而是 `minidb-protocol` 的 `SchemasRequest`/`TablesRequest`/`ColumnsRequest` 三条独立消息(响应复用 `ArrowBatch`)。服务端 `MetadataExecutor`(外挂,持 `catalog+allocator`,不依赖 storage/stats)从 `MiniDbCatalog` 物化 Arrow 行,`SessionHandler.handleMetadata` 走现有 `sendRows`。`getSchemas` 的 `TABLE_CATALOG` 恒 null、`getTables`/`getColumns` 的 `TABLE_CAT` 恒 null(MiniDB 无 catalog 概念,`getCatalog()=null`);`NULLABLE` 恒 1(列全可空);`getColumns` 24 列完整 JDBC 规范,无语义列填默认(`COLUMN_SIZE=0`/`NUM_PREC_RADIX=10`仅整数/`IS_NULLABLE="YES"` 等)。LIKE 过滤(`_`/`%`)在 `MetadataExecutor.compileLike` 转正则,`null` pattern 跳过过滤。
20. **标识符引号用双引号**——驱动 `getIdentifierQuoteString()` 返回 `"`,SQL IDE(DBeaver 等)据此生成 `select * from "public"."t"` 形式 SQL。Calcite parser 必须 `withQuoting(Quoting.DOUBLE_QUOTE)`(Lex.MYSQL 默认只认反引号,IDE SQL 会报 `ParseException: Encountered "\""`)。代价:**反引号标识符不再支持**(Quoting 是单值枚举,无双引号+反引号共存;现有代码/测试无反引号依赖)。
21. **Calcite 1.42 的 `AggregateCall` 参数存储**:纯列引用参数(如 `SUM(id)`)时 `rexList` 为空、索引存 `argList`;表达式参数(如 `SUM(id*2)`)才填 `rexList`。聚合算子取参数必须两个都查(先 `rexList` 后 `argList` 索引取输入列)。只查 `rexList` 会静默丢掉参数(SUM 恒 NULL,COUNT 退化为 COUNT(*))。
22. **Calcite 1.42 聚合类型推导**:`SUM(INTEGER)`→INTEGER(非 BIGINT)、`AVG(INTEGER)`→INTEGER(截断)、`SUM(DOUBLE)`→DOUBLE、`COUNT`→BIGINT、`MIN/MAX`→同参数。实现按 `Aggregate.getRowType()` 落地,断言/测试别假设 SUM(INT)→BIGINT。
23. **既有 bug:含 DOUBLE 字面量的 VALUES INSERT 失败**(与 aggregate 无关):Calcite 对多行 `VALUES (1, 10.5), ...` 或含 CAST 的单行生成 `LogicalUnion`(每行一个 `Project(CAST)` over 占位 `Values`),MiniDB 无 Union 规则 → `CannotPlan`。**已修**:NULL/字面量对 INTEGER 不再生成 BigIntVector——`literalVector`/`nullLiteral` 现按类型映射 INTEGER→IntVector、BIGINT→BigIntVector(此前的 BigIntVector 统一导致复制到 IntVector 列时 `RowCopier.copyRow` 抛 MinorType 不一致,并催生一堆跨型重载)。**可行的数据构造**:单列任意类型逐行/多行 INSERT、多列多行同型(无 CAST)INSERT、`UPDATE ... SET col = NULL`(走 `writeValue` 类型转换路径)。
24. **DISTINCT 的实现落点**:`SELECT DISTINCT` 是 Calcite 规划的 `MiniDbAggregate(groupSet=全列, aggCalls=[])`,上轮分组实现天然支持(无需新代码);`COUNT(DISTINCT x)` 等是 `AggregateCall.isDistinct()`,需在 factoryFor 分流到 `DistinctAcc`(LinkedHashSet 去重,NULL 不参与)。注意:**只测 `COUNT(id)` 无法发现 distinct 未实现**(表行数碰巧等于去重数时会掩盖),要用含重复数据 + 期望去重后的值。
25. **UNION 执行顺序坑**(MiniDbUnion):先收集所有输入 batches,**merge 完成后**才 `close()` 输入迭代器——Project/Filter 的 close 释放自己 owned 的 batch,先 close 会导致向量 valueCount 归零(IndexOutOfBounds 或空结果)。Sort 同款:merge 后 close。
26. **`VectorSchemaRoot.of(vectors)` 的 rowCount 取第一个 vector 的 valueCount**——必须先 `v.setValueCount(dst)` 再 `of()`,否则 root.getRowCount() 恒 0(向量有数据但 root 显示 0 行)。Aggregate/Union 都是先 setValueCount 后 of。`root.setRowCount(n)` 会同时设置 root.rowCount 和所有 vector 的 valueCount。
27. **Calcite 的 `Union` 用公共字段 `all`**,无 `isAll()` getter——引用时用 `union.all`(LogicalUnion 同样)。
28. **UNION 去重实现**:Calcite 1.42 不注册 UnionToDistinctRule,`UNION` 保持 `LogicalUnion(all=false)`,去重在 MiniDbUnion 内部做(`LinkedHashSet<List<Object>>` 行级,规范化值含 null)。UNION 两侧列名不同没关系(RowCopier.copyRow 按索引拷),但类型必须兼容(Calcite 已校验/CAST)。
29. **SetOp 的 kind 语义**:Calcite `SetOp` 自带 `public final SqlKind kind` + `public final boolean all`,构造器 `(cluster, traits, inputs, kind, all)`。`INTERSECT`/`INTERSECT ALL` 的 kind 都是 `SqlKind.INTERSECT`(无 INTERSECT_ALL 枚举,靠 all 区分);`EXCEPT` 解析为 `LogicalMinus`,kind=`SqlKind.MINUS`(EXCEPT 是 MINUS 别名)。INTERSECT/EXCEPT 不去重展开,语义(去重/ALL 计数)在 MiniDbSetOp 内部完成。
30. **LogicalCalc 在 MiniDB 规划下不出现(Filter/Project 标准路径)**:常规 SQL 全部规划为 LogicalProject/LogicalFilter,无 LogicalCalc;`MiniDbCalc` 是防御性算子(未来加 CalcMergeRule 或 Calcite 版本变化时用)。Calc 的 RexProgram 手动构造(测试用)注意:`RexProgramBuilder.addProject(RexNode,String)`/`addCondition(RexNode)`,无 register 方法;`RexBuilder` 无 `makeLiteral(int)`,用 `makeExactLiteral(BigDecimal, type)` 保类型;`SqlStdOperatorTable` 在 sql.fun 包。
31. **JOIN 关键点**:Calcite 对 INNER/LEFT/RIGHT/FULL/非等值都规划为 `LogicalJoin(condition, joinType)`,列索引跨两侧;`JoinInfo.of(left,right,condition)` 现成等值对分析。join 输出顺序随算法不同(HASH 右驱动、SORT_MERGE 排序序),测试断言前必须排序。NULL 键等值永不匹配(SQL 语义),三种算法都要显式处理。`RexInterpreter.comparison` 原只支持数值域,字符串列比较(join 条件/Filter 字符串等值)会 asLong 抛异常——已加 stringDomain 分支(toString 比较)。`COALESCE` 解析为 CASE,RexInterpreter 不支持(既有缺失)。
32. **窗口函数**:Calcite 1.42 把窗口函数放在 **Project 表达式里的 RexOver**(无 LogicalWindow 节点,除非优化规则);空分区保护包成 `CASE(>(COUNT(x) OVER (...), 0), SUM(x) OVER (...), null)`。ProjectToWindowRule 单独注册 NPE(缺 RelOptSchema)、与 ConverterRule 协同不触发——**别走那条路**,在 MiniDbProject 里检测 RexOver 后 eager 提取(visitOver 改写为窗口列引用)再 eval。窗口实现=WindowFunctions 静态工具。RexFieldCollation 无 getFieldIndex,用 `((RexInputRef) fc.left).getIndex()`。**两个易漏的坑(乱序输入才暴露,排序好的测试数据会掩盖)**:① `WindowFunctions.comparator` 的 ORDER BY 键必须是**主排序键**,行索引只作 `thenComparingInt(i -> i)` 的稳定 tiebreak——写成 `comparingInt(i->i).thenComparing(键)` 会让行索引成主键、ORDER BY 被忽略;② `computeOver` 结果要按**原始行索引**落位(`result[originalRowIdx] = ...`),不能用 `ArrayList.add(行索引, 值)` 把行索引当插入位置(乱序越界/错位)。
33. **EXPLAIN trivial Project 折叠**:`ExplainExecutor` 原无条件折叠所有 MiniDbProject(列选择),窗口 Project(非 identity)也被折叠 → EXPLAIN 无 Project 行。已改为 `isTrivialProject`(全 RexInputRef 且索引连续才折叠)。
34. **RexInterpreter 的 CASE**:窗口 null 保护 `CASE(cond, then, else)` 需要求值;caseExpr 按行选第一个为真的分支,输出向量类型 = call.getType()(newVector 按 SqlTypeName 建 Int/BigInt/Float8/VarChar/Bit/Date/Timestamp)。同时覆盖 `COALESCE`(解析为 CASE)——坑 31 里说的 CASE 缺失已修复。
35. **HepPlanner 无 `(program, cluster)` 构造器**——签名是 `(HepProgram)` / `(HepProgram, Context)` / `(HepProgram, Context, boolean, ...)`,没有 `(HepProgram, RelOptCluster)`。逻辑优化阶段不能复用 VolcanoPlanner 的 cluster,只能 `new HepPlanner(program)` 自建 context(`LogicalOptimizer.optimize` 即如此),`setRoot(logical)` + `findBestExp()` 返回优化后逻辑树。
36. **Calcite 1.42 内置规则无静态 `INSTANCE`**——`FilterProjectTransposeRule`/`ProjectMergeRule`/`FilterMergeRule` 等规则类没有 `XxxRule.INSTANCE` 静态字段,用 `XxxRule.Config.DEFAULT.toRule()` 创建(`MiniDbLogicalRules.ALL` 即此);`FilterJoinRule` 用 `new FilterJoinRule.FilterIntoJoinRule(false, RelFactories.LOGICAL_BUILDER, TRUE_PREDICATE)`。`ConverterRule` 同理走 `Config.INSTANCE.withConversion(...).withRuleFactory(...)`。
37. **`RelMetadataQuery.collations()` 替代 `getCollation`,且查逻辑树**——1.42 的 `RelMetadataQuery` 只有 `collations(RelNode)`(返回 `List<RelCollation>`,节点可能声明多个),没有 `getCollation`。join 有序性判断经 `MiniDbJoin.coversKeys` 检查是否有 collation 升序前缀覆盖 join 键(`MiniDbSortMergeJoin` 构造用;`RelMdCollation.collations(RelSubset)` 返回 RelSubset traitSet 里的 collation,故转换后的物理子节点也能查到有序性)。
38. **collation trait 注册时机(先于 cluster 创建)**——`RelCollationTraitDef.INSTANCE` 必须**在 `RelOptCluster.create(planner, ...)` 之前** `planner.addRelTraitDef(...)`,否则 RelNode 的 traitSet 缺 collation 分量,trait 不匹配抛 AssertionError。`Planner` 顺序:`addRelTraitDef(ConventionTraitDef)` → `addRelTraitDef(RelCollationTraitDef)` → `addRule` 全部 → 再 `RelOptCluster.create`。
39. **`MiniDbProject`/`MiniDbFilter` 物理层暂不声明 collation**——这两个物理算子构造不替换 collation trait(仅透传规则传入的 traitSet),不声明输出排序;join 决策的有序性判断完全靠逻辑树侧 `RelMetadataQuery.collations()`(RelMdCollation 从 Sort/Project/Filter 派生,Scan 等默认 EMPTY),物理节点无需额外实现(设计 spec 的"物理节点声明 collation"未落地,以 RelMetadataQuery 为准)。
40. **派生表 ORDER BY 无 LIMIT 可能被优化掉**——子查询 `(SELECT * FROM t ORDER BY a)` 不带 LIMIT 时 ORDER BY 无语义,Calcite 规划会丢弃 Sort(子查询并入 join 后输入无 collation,RelMdCollation 返回 EMPTY)→ join 选不了 SortMergeJoin。要保输入有序必须带 `LIMIT n`(CollationJoinTest 用 `ORDER BY id LIMIT 1000`)。
41. **ProjectRemoveRule / CalcRemoveRule 会丢列别名,不要注册**:Calcite 这两个规则按「索引恒等」(RexInputRef 索引==位置)判 trivial,不看字段名。`SELECT a.id AS aid` 的 Project/Calc 索引恒等但改了列名,被当 no-op 删除 → 结果 schema 丢 `aid`,JDBC 客户端拿不到别名。MiniDB 暴露列名给客户端,这两个规则必须禁用。
42. **排序移除规则的分裂放置(Calcite 1.42 的坑)**:`SortRemoveConstantKeysRule` 因 `Bug.CALCITE_6611_FIXED=false`,只在**无** `RelCollationTraitDef` 的 HepPlanner 阶段触发(有 trait 反而 return 跳过),所以放 `MiniDbLogicalRules.HEP`;`SortRemoveRule` 恰恰相反——只在**有** `RelCollationTraitDef` 的 VolcanoPlanner 阶段触发,放 `MiniDbLogicalRules.SORT`。放反了规则静默失效(常量键 Sort 不删、输入有序 Sort 不删)。`SortRemoveDuplicateKeysRule`/`SortRemoveRedundantRule` 不依赖 trait,也在 HEP。
43. **`FilterProjectTransposeRule` + `ProjectFilterTransposeRule` 互为逆操作,同时注册会振荡 → StackOverflowError**:两者一个把 Filter 推到 Project 下、一个把 Project 推到 Filter 下,`Filter(Project)` → `Project(Filter)` → `Filter(Project)` 无限循环(HepPlanner 的 rel digest 去重挡不住,因为每轮字段映射不同)。只留 `FilterProjectTransposeRule`(filter 下推方向),不要加 `ProjectFilterTransposeRule`。
44. **新增物理算子时要同步补优化规则**:每加一个算子(如 Window/Correlate/TableModify 变体/多值等),要检查 CoreRules 里对应的换位/下推/合并规则(如 `FilterAggregateTransposeRule` 之于 Aggregate、`FilterSetOpTransposeRule` 之于 SetOp、`FilterWindowTransposeRule`/`ProjectWindowTransposeRule` 之于 Window),否则该算子周围永远少一层优化。反之,加规则前要确认物理算子能承接规则产出的形状(如规则会产 SEMI join 但 MiniDbJoin 抛错)。
45. **递归 CTE(WITH RECURSIVE)的落点**:Calcite 把非递归 CTE 在 `SqlToRelConverter.convertWith` 直接内联成子查询(**零代码即支持**);递归 CTE 才生成 `LogicalRepeatUnion(seed, iterative)` + 两个 `LogicalTableSpool`(共享同一个瞬态 `RelOptTable`,qualified name 只有 1 段,如 `["nums"]`),迭代体里 `TableScan(nums)` 读回瞬态表。执行语义(对齐 SQL 标准):working 表初始=seed(`all=false` 先去重),每轮 materialize 迭代体(读 working)得到 produced,**去重后** append 到 result、working=去重后的新行,直到 produced 空或全重复(不动点)或达 `iterationLimit`。关键实现点:① 瞬态表用 `ExecContext` 的 `Map<String,List<Object[]>>` 传递(`MiniDbRepeatUnion` 写、`MiniDbScan` 读),不是 storage;② `MiniDbScan` 靠 **qualified name 段数==1** 识别瞬态表(真表是 2/3 段,见坑 17),避免与同名真表冲突;③ `MiniDbTableSpool` 是纯透传(spooling 由 RepeatUnion 驱动),只为让 `LogicalTableSpool` 能转 MINIDB 约定;④ `RepeatUnion.copy` 用 `(RelTraitSet, List<RelNode>)` 签名、`Spool.copy` 用 `protected (RelTraitSet, RelNode, readType, writeType)`(Spool 的 public `copy` 是 final,委托 protected)。`all`/`iterationLimit`/`transientTable` 都是 RepeatUnion 的 public final 字段。
46. **Calcite 字符串字面量默认字符集是 ISO-8859-1**——系统属性 `calcite.default.charset` 默认 `"ISO-8859-1"`,`SqlTypeFactoryImpl.getDefaultCharset()`(经 `Util.getDefaultCharset()`)返回它。含中文等非 Latin1 字符的字面量(`INSERT ... VALUES ('张总')`、`WHERE name='张总'`)在 `SqlToRelConverter` → `RexBuilder.makeCharLiteral` → `NlsString` 构造时抛 `Failed to encode '张总' in character set 'ISO-8859-1'`。修法:子类 `Utf8SqlTypeFactory extends SqlTypeFactoryImpl` 覆写 `getDefaultCharset()` 返回 `StandardCharsets.UTF_8`,`Planner`/`CalciteContext` 建类型工厂处改用之(VARCHAR 本就用 UTF-8 字节存储)。别用 `-Dcalcite.default.charset=UTF-8` 系统属性——`Util.DEFAULT_CHARSET` 是静态 final 字段,初始化时机不可控。另 `SqlParser.Config`(1.42)无 charset 字段,改 parser config 无效。
47. **列式标量函数框架(`exec/functions` 包)**:算术(`+ - * /`)/比较(`= != < <= > >=`)/字符串(`UPPER`/`LOWER`/`TRIM`/`CHAR_LENGTH`/`LENGTH`/`||`/`CONCAT`/`SUBSTRING`)/数学(`ABS`/`FLOOR`/`CEIL`/`ROUND`)不再内联在 `RexInterpreter`,走注册表。组件:`Kernel`(Tier-2 完整核 `void execute(List<ValueVector>, FieldVector)`)/`ScalarKernels`(Tier-1 无装箱 lambda 接口)/`Kernels.fill*`(共享 STRICT-null 循环,只写值**不 setValueCount**)/`Overload`(record `inputTypes + outputType + kernel`,**输出类型参与分发**)/`Function`(若干 overload,按输入+输出类型分发)/`FunctionRegistry`(`Map<SqlOperator,Function>`,可变,UDF 挂载点)/`BuiltInFunctions`(注册全部内置)。`RexInterpreter.evalCall` default 分支:先 `eval` 各操作数成向量,再 `functions.lookup(call.getOperator())`,`f.evaluate(args, call.getType(), allocator)`。`Function.evaluate` 负责:`outputVectorClass(resultType)` 把 Calcite 类型映成箭头向量类 → 逐 overload 匹配 `inputTypes`(isInstance)+ `outputType`(equals)→ `ArrowTypes.field(resultType,"expr").createVector(allocator)` 分配 → try 内 `kernel.execute` → finally 关闭入参 → **最后 `out.setValueCount(rows)`**。三个坑:① **register-overwrite**——`register` 用 `Map.put`,一个 `SqlOperator` 只对应一个 `Function`,所有重载必须收进那一个 `Function` 的 `overloads` 列表,逐个 register 会互相覆盖;② **真混型 INTEGER/BIGINT 需跨型重载**——`int_col + bigint_col`/`int_col > bigint_col` 时 Calcite **不**强制 CAST,操作数保持 Int+BigInt 混型、结果 BIGINT,必须注册 `[Int,BigInt]`/`[BigInt,Int]` 跨型重载(int 侧 promote 到 long、输出 BigIntVector)。注意与字面量无关:整数字面量经 `literalVector` 已是 IntVector(坑 23 已修,不再产 BigIntVector)。**CAST 行为因算子而异(易踩坑)**:比较(`=`/`>` 等)对跨 family(INT↔DOUBLE/DECIMAL)会插 CAST 到同型(探针见 `>(CAST($0):DOUBLE,$1)`),故比较只需处理同 family 的 INT↔BIGINT 混型(不 CAST);算术(`+-*/`)**不插 CAST**(`+($0,$1)` 直接混型),故算术还必须为跨 family 注册重载——整型×DOUBLE→DOUBLE、整型×DECIMAL→DECIMAL、DOUBLE×DECIMAL→DOUBLE,统一经 `BuiltInFunctions.fillDoubleMixed`/`fillDecimalMixed` 把两侧提升到 double/BigDecimal 再按更宽类型算(复用 `doubleKernel`/`decimalKernel`);③ **输出类型进 Overload 是为将来留的**——当前函数集每个「输入组合→输出」唯一,`outputType` 实际冗余,但保留它让「同一输入类型映射多种输出」的函数(未来 UDF)能被正确分发。**CAST 与字面量已标准化**:`RexInterpreter.literalVector`/`nullLiteral` 按类型映射 INTEGER→IntVector、BIGINT→BigIntVector(不再统一 BigIntVector);`evalCast` 产正确输出类型(INTEGER→IntVector、BIGINT→BigIntVector、DOUBLE→Float8、VARCHAR→VarChar、BOOLEAN→Bit)并支持数值↔字符串互转(`asLong`/`asDouble` 会 parse 字符串、`asString` 把数值转字符串)。**TRIM 是专用 handler**:Calcite 把 `TRIM(s)` 解析期重写为 3 参 `TRIM(Flag,' ',s)`,Flag 是 SYMBOL 字面量(经 `RexBuilder.makeFlag` 产出、`RexLiteral.getValueAs(SqlTrimFunction.Flag.class)` 可取),不是列值、走不了常规字面量向量,故 `RexInterpreter.evalCall` 专 `case TRIM` 直接从 RexLiteral 取 Flag 再求值 trim 字符集+输入串。**SQL 关键字映射坑**:`LENGTH(name)`/`CONCAT(a,b)` 函数语法默认不被识别——它们是 `SqlLibraryOperators` 的库函数(`LENGTH` 属 POSTGRESQL 等、`CONCAT_FUNCTION` 属 MYSQL),要支持必须在 `CalciteContext` 用 `SqlLibraryOperatorTableFactory.INSTANCE.getOperatorTable(SqlLibrary.STANDARD, SqlLibrary.MYSQL, SqlLibrary.POSTGRESQL)` 替换 `SqlStdOperatorTable.instance()`,**漏掉 `STANDARD` 会丢掉 `+`/`>` 等所有标准算子**(getOperatorTable 只返回显式列出的库);执行侧再在 `BuiltInFunctions` 注册 `SqlLibraryOperators.LENGTH`(= `CHAR_LENGTH.withName("LENGTH")`,与 `CHAR_LENGTH` 是不同 key,各自注册)和 `CONCAT_FUNCTION`(变参,注册 1/2/3 参重载共用一核)。**字符串/数学均按 SQL 标准语义实现**:`LENGTH`/`CHAR_LENGTH`/`SUBSTRING`/`TRIM` 按 Unicode **code point**(非 UTF-16 code unit)计与截取(`codePointCount`/`codePoints()`);`SUBSTRING` 的 offset/length 用 long 传入、在 `slice` 内 clamp 到 [0, codePointCount](不 int 截断回绕);`ROUND` 用 round-half-away-from-zero(`sign(x)*floor(|x|+0.5)`,非 `Math.rint` 的 tie-to-even)。
48. **Calcite 列名大小写敏感由 `CalciteCatalogReader` 的连接配置控制,不在 `SqlValidator.Config`**——`SqlValidator.Config` 根本没有 `withCaseSensitive` 方法(compile 直接报错)。`CalciteConnectionConfigImpl.caseSensitive()` 回退到 `Lex` 默认值(`Lex.ORACLE` 是 `caseSensitive=true`),所以即便 `SqlParser.Config` 设了 `caseSensitive(false)`(那只作用于 parser),校验期 `information_schema.tables` 的 `TABLE_NAME` 大写列仍无法用小写 `table_name` 查询,报 `Column 'table_name' not found; did you mean 'TABLE_NAME'?`。修法:在 `CalciteContext.buildCatalogReader` 构造 `CalciteConnectionConfigImpl` 前 `props.setProperty(CalciteConnectionProperty.CASE_SENSITIVE.camelName(), "false")`。坑点:这个标志在「连接配置」这一层,不在 parser/validator 配置,极易找错地方。
49. **元数据独立持久化(catalog.json)与空表存活**——`TableSchema` 持久化到 `data/catalog.json`(JSON,`CatalogStore` 接口 + `JsonCatalogStore` 实现,Avro 将来加实现类),独立于 `.arrow` 数据文件,解决「空表(从未 INSERT)无 `.arrow` 文件、重启即丢」。三个关键点:① `MiniDbCatalog.restore()` 必须**不触发** `notifyChange`(否则 `loadAll` 把刚读到的文件原样写回,且加载期 listener 可能重复写);② `StorageManager.loadAll()` 先 `restoreCatalog()`(有 catalog.json → `catalog.restore` 并返回 true,否则回退 `.arrow` 推断返回 false)再遍历 `.arrow` 加载数据,`loadFile(restored=true)` 时表 schema 从 catalog 取、**不重复 `catalog.createTable`**(否则抛 "table already exists");旧目录无 catalog.json 回退旧行为(`restored=false`);③ catalog.json 由 `StorageManager` 构造时挂的 `catalog.addListener(this::persistCatalog)` 在每次 DDL(create/drop schema/table)后同步写盘(原子写:临时文件 + `Files.move(ATOMIC_MOVE)`),无需在 `close()` 里额外 flush。
50. **information_schema 系统表接线**:`information_schema` 是 `MiniDbCatalog` 里的**平级 schema**(构造时注册,表定义在 `catalog/InformationSchemaCatalog`),Calcite 树统一从 `catalog.schemaNames()` 派生(无特判)。`MiniDbScan.execute()` 在 3 段限定名里 `schemaName` 命中 `InformationSchemaCatalog.isSystemSchema` 时走 `exec/InformationSchema.materialize` 单批分支(复用 `singleBatch` 包装)——系统表是「虚拟表」,行在查询时物化,`StorageManager` 里没有对应 ArrowTable。`QueryExecutor.handleCreateSchema`/`handleDropSchema` 拒绝 `information_schema` 保留名,`MiniDbCatalog.dropSchema` 同样保护;`snapshot()` 排除系统 schema(不持久化)。物化**自描述**:`schemata` 列 `information_schema` 自己、`tables`/`columns` 列 3 张系统表自身。注意:大量 NULL 列(如 `CATALOG_NAME`)靠「只对有值列 `setSafe`、全部列 `setValueCount(n)`」实现(先 setValueCount 再 `VectorSchemaRoot.of`,见坑 26)。
51. **JDBC 元数据 LIKE 的转义字符 `\` 必须处理**(`MetadataExecutor.compileLike`):驱动 `getSearchStringEscape()` 返回 `\`,DataGrip/DBeaver 等工具把 schema/表/列名里的 `_` 转义成 `\_` 再传 `getSchemas`/`getTables`/`getColumns` 的 pattern,以匹配**字面**下划线。若 `compileLike` 只把 `%`→`.*`、`_`→`.` 而不处理 `\X` 转义,`information\_schema` 会被拆成「字面反斜杠 + 通配」,匹配不到真实的 `information_schema` → 界面点开 information_schema 显示空表。修法:遍历时遇 `\` 取下一个字符按字面量输出(`\_`→字面 `_`、`\%`→字面 `%`、`\\`→字面 `\`),其余字符 regex 转义。
52. **Calcite 1.42 的 `VolcanoCost.isLt` 只比较 rowCount 分量,cpu/io 被忽略**(`VolcanoCost.java` 里 `isLt` 有个 `if (true)` 占位短路,是 Calcite 多年未实现的 TODO)。代价选 join 算法时若照常规写 `makeCost(rows, cpu, 0)` 把工作量放 cpu,三种 join 算法的代价会退化成相等(都等于 join 输出行数),VolcanoPlanner 无法区分。修法:把「工作量」编码进 rowCount 分量 `makeCost(work, 0, 0)`(NestedLoop=`left×right`、Hash=`left+right`、SortMerge=`left+right+sort`),代价才算得出高低。`MiniDbHashJoin`/`MiniDbSortMergeJoin`/`MiniDbNestedLoopJoin` 的 `computeSelfCost` 都这么写并加 WHY 注释。代价排序里三个算法共享同一输入,子节点累计代价相互抵消,相对排序只取决于 self-cost 差。
53. **`ReduceExpressionsRule`(常量折叠)用 `RexExecutorImpl`(Janino 编译 `SqlFunctions`)在计划期折叠常量函数调用,会绕过 `exec/functions` 的 kernel**。若内核语义与 Calcite 的 `SqlFunctions` 不一致,常量与列求值会分歧。实例:`CHR(0)` 内核原返 null(仿 PostgreSQL),但 Calcite `charFromUtf8(0)` 返 `"\0"`(NUL 字符),折叠后 `SELECT CHR(0)` 拿到 `"\0"` 而 `SELECT CHR(n)`(n 为列)拿到 null。修法:内核对齐 Calcite 语义(此处返回 NUL 字符,VARCHAR 能存 NUL,无需 PostgreSQL 那套「存储受限才返 null」)。新增标量函数时:要么对齐 Calcite `SqlFunctions` 的语义,要么确认该函数无 `SqlFunctions` 折叠实现(否则常量走折叠、列走内核,行为漂移)。
54. **视图(VIEW)落点**:视图定义 `ViewDefinition(schemaName, name, querySql, columns)` 存 `MiniDbCatalog` 的 views map,随 `CatalogSnapshot` 持久化到 catalog.json。DDL 走 Calcite 原生 `SqlCreateView`/`SqlDropView`(解析器 `SqlDdlParserImpl` 原生支持,无需前缀拦截;`SqlCreateView` 无 IF NOT EXISTS、只有 replace 标志)。查询侧:视图在 `MiniDbCalciteSchema.tableMap` 里用 Calcite `ViewTable`(TranslatableTable)暴露,`SqlToRelConverter` 展开时必须传**非 null 的 `ViewExpander`**(原传 null,遇视图 NPE)。`ViewExpander` 放在 `Planner`(非 `CalciteContext`),捕获本次规划的 `VolcanoPlanner` + `typeFactory`——展开视图定义 SQL 时用**同一个 planner** `RelOptCluster.create` 重建 cluster,保证展开出的 RelNode 的 traitSet 与外部树一致(否则缺 convention/collation 分量,`changeTraits` 抛 AssertionError,坑 38 的延伸)。`expandView` 取 `schemaPath` 最后一段作视图所在 schema,递归 `planInCluster(queryString, cluster, schema, this)` 支持视图套视图。`CREATE VIEW v(a,b)` 显式列名已支持(数量匹配则按列表重命名列);**可更新视图(`INSERT INTO view`)暂不支持**——需移植 Calcite 的 `Schemas.analyzeView`(标注 "For use within Calcite only",要 `CalciteConnection`/`CalciteSchema`/`CalcitePrepare`)+ `Path` + `ModifiableViewTable`,MiniDB 无此 prepare 上下文,属独立大功能。
55. **约束(主键/唯一/NOT NULL)落点**:约束元数据存 `TableSchema`(`primaryKey`/`uniqueKeys`,空=无)+ `ColumnMeta.nullable`(默认 true,旧 catalog.json 归一化),随 catalog.json 持久化。`MiniDbCalciteTable.getStatistic()` 把主键+唯一约束转成 `ImmutableBitSet` 作唯一键(**与统计无关,未 ANALYZE 也提供**),供 CBO 的 `RelMdUniqueKeys`→`RelMdDistinctRowCount` 得到更准的 distinct/join 基数。DDL:表级 `PRIMARY KEY (col)`/`UNIQUE (col)` 是 Calcite 原生 `SqlKeyConstraint`(用 `key.getOperandList().get(1)` 取列名,`getColumnList()` 不存在);**列级 `col TYPE PRIMARY KEY`/`UNIQUE` Calcite 不支持**,`QueryExecutor.rewriteColumnConstraints` 在 parse 前用字符串扫描(跟踪括号深度/引号)把列级约束提升为表级。执行期校验在 `MiniDbModify.validateInsert`(INSERT 前):NOT NULL + 主键/唯一冲突,含 null 的键不参与唯一性(唯一约束允许多 null);校验失败走 try-finally 关闭输入迭代器(否则 Arrow 内存泄漏)。**外键(FOREIGN KEY/列级 REFERENCES)已支持**:`ForeignKey(columns, refSchema, refTable, refColumns)` 存 `TableSchema.foreignKeys`;Calcite 无外键 DDL,`rewriteColumnConstraints` 在 parse 前剥离表级 FOREIGN KEY 与列级 REFERENCES(支持 schema 限定、省略引用列=引主键)。执行期:INSERT/UPDATE 校验外键值存在于引用表、DELETE 校验被删行主键不被引用(RESTRICT,只处理引用本表主键的常见情况);引用非主键唯一列与 UPDATE 改被引用列的外键校验暂未覆盖。
56. **`SELECT * ORDER BY <expr>` 的 ORDER BY 临时列泄露**——`Planner.plan` 原先只用 `root.rel`,丢弃了 `RelRoot.project` 的列裁剪信息。`SqlToRelConverter` 对 `SELECT * ORDER BY (a-b)` 会把 `a-b` 作为额外列挂在 Sort 输入上(因 `a-b` 不在结果列里,无法用 collation 表达),`RelRoot.fields` 用 `(索引,列名)` mask 告诉消费者丢弃它。修法:`Planner.plan` 改用 `root.project()`(非 `root.rel`)——`project()` 在 `isRefTrivial()`(fields 是 identity 映射)时返回原 rel,**不破坏坑 41 的列别名保留**(别名场景 fields 仍 trivial);仅当 fields 非平凡(需裁掉 ORDER BY 临时列)才创建 `LogicalProject` 裁剪。禁用的 `ProjectRemoveRule` 只按「索引恒等」判 trivial、无法处理这种「列数减少」的裁剪,故必须在 plan 入口显式 `project()`。实例:TPC-DS query36/47/57 的 `order by sum_sales - avg_monthly_sales` / `order by case when lochierarchy=0 then i_category end` 会泄露成 `EXPR$N` 输出列。
57. **窗口 `AVG(DECIMAL)` 被 Calcite 重写为 `SUM/COUNT` 除法 + 外层 CAST 截断精度**——Calcite 把 `avg(sum(decimal)) over(...)` 的 null 保护重写为 `CAST( (CASE(>(COUNT over,0), SUM over / COUNT over, null)) : DECIMAL(原scale) )`。内部 `SUM over`(Decimal(19,2))/ `COUNT over`(BigInt)除法经 `fillDecimalMixed` 得 scale=6 高精度结果,但外层 `CAST(:DECIMAL(19,2))` 在 `VectorCasts.cast` 的 DECIMAL 分支按目标 scale=2 建向量、`scaleTo` 截断,丢精度(TPC-DS query47/53/57)。该 CAST 是 Calcite 自动插入的类型对齐(让除法结果匹配 AVG 声明的返回类型),非用户显式截断意图。修法:`VectorCasts.cast` DECIMAL 分支,当源 `DecimalVector.scale > 目标 scale` 时保留源 scale(精度优先),precision 相应放大不超 38。注意:这与 `RexInterpreter.newVector` 的「DECIMAL scale 提升到至少 6」、`MiniDbAggregate.buildOutput`/`MiniDbProject.buildWindowBatch` 的 AVG 精度提升是同一类问题(DECIMAL 精度提升)的不同落点——Calcite 不为 AVG 提升 scale,需在物理层各路径补。DuckDB 对 AVG 返回 DOUBLE,与 Calcite 的 DECIMAL 返回类型不同,正确性测试用相对容差比较(浮点容差 1e-6)。
58. **`JoinReorderer` 贪心只看连接度、平手取首见,大表当选种子把非等值 NestedLoop 推到大表 join 结果上(TPC-DS query8)**——`JoinReorderer.greedyRebuild` 原按等值连接图边数选种子、每步加连接数最多的叶子,平手时不更新(取下标最小)。query8 连接图:`store_sales`(度2,12万行)与 `store`(度2,2行)平手,取下标0的 `store_sales` 当种子,非等值条件 `substr(s_zip)=substr(ca_zip)` 只能落到最后加入的 `V1` 所在 join,接在 `((store_sales⨝date_dim)⨝store)` 大表结果(1096万行)×V1 之间 → NestedLoop 2.3 亿次 RexInterpreter 求值。**代价公式没错**(坑52的 rowCount 编码正确反映了灾难性顺序),错在顺序。修法:种子与每步选择在连接度平手时按「叶子行数最小」破平手(`RelMetadataQuery.getRowCount`,null 当 1e8),让 `store`(小)赢过 `store_sales`(大),非等值落到 `store×V1`(42行)间,再用 HashJoin 接大表——求值次数从 2.3亿降到约 424。**关键兜底**:无统计(未 ANALYZE)时 Calcite 1.42 的 `RelMdUtil.estimateFilteredRows` 对 null selectivity 直接 unboxing 抛 NPE(`multiply` 返回 null,作者用 `@SuppressWarnings("unboxing.of.nullable")` 知道但没防),`rowCount()` 必须 try-catch 回退 1e8——回退后所有叶子同值、行数破平手退化为不生效,贪心回到纯连接度决策,与修复前一致(不回退 query10/18 的交叉连接消除)。**流式 NestedLoop 摘到后续**:query8 修好顺序后匹配对极少,不需流式;流式(`joinPairs`→迭代器,改 3 子类+基类+outer join 语义)留作防御性后续任务。`JoinReorderNonEquiTest` 回归:大表+小表+非等值,断言非等值 join 左叶子≠大表。

## 文档与计划

- `README.md` — 用户向说明(特性/类型/限制)。
- `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md` — 设计 spec(brainstorming 产出)。
- `docs/superpowers/plans/YYYY-MM-DD-<topic>.md` — 实现计划(writing-plans 产出)。
- `.claude/settings.local.json` — 本地权限白名单。
- `.superpowers/sdd/` — SDD 工作区(gitignored,临时 ledger/brief/report,合并后可删)。
